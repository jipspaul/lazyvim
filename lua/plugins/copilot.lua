return {
  "github/copilot.vim",
  config = function()
    -- Copilot will be automatically configured after installation
    -- You may need to run :Copilot setup after the plugin is installed
  end,
}